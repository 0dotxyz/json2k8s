apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-name-subdomain-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - app-name.stage.mrgn.app
        - stage.mrgn.app
      secretName: app-name-tls
  rules:
    - host: app-name.stage.mrgn.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app-name
                port:
                  number: 8080
